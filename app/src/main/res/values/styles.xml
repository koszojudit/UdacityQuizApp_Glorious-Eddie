<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="question_title_style">
        <item name="android:layout_margin">16dp</item>
        <item name="android:fontFamily">sans-serif-light</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/question_title_textSize</item>
    </style>

    <style name="question_desc_style">
        <item name="android:layout_marginLeft">@dimen/edit_desc_marginLeft</item>
        <item name="android:layout_marginRight">@dimen/edit_desc_marginRight</item>
        <item name="android:layout_marginBottom">@dimen/edit_desc_marginBottom</item>
        <item name="android:fontFamily">sans-serif-light</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/question_desc_textSize</item>
    </style>

    <style name="edit_text_style">
        <item name="android:layout_marginLeft">@dimen/edit_text_margin</item>
        <item name="android:layout_marginRight">@dimen/edit_text_margin</item>
        <item name="android:layout_marginBottom">@dimen/edit_text_margin</item>
    </style>

</resources>
